<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="MarqueeViewStyle">
        <attr name="mvInterval" format="integer|reference" />
        <attr name="mvAnimDuration" format="integer|reference" />
        <attr name="mvTextSize" format="dimension|reference" />
        <attr name="mvTextColor" format="color|reference" />
        <attr name="mvGravity">
            <enum name="left" value="0" />
            <enum name="center" value="1" />
            <enum name="right" value="2" />
        </attr>
    </declare-styleable>

    <declare-styleable name="SegmentedGroup">
        <attr name="sc_corner_radius" format="dimension" />
        <attr name="sc_border_width" format="dimension" />
        <attr name="sc_tint_color" format="color" />
        <attr name="sc_checked_text_color" format="color" />
        <attr name="sc_unchecked_tint_color" format="color" />
    </declare-styleable>

    <declare-styleable name="AwesomeRadioButton">
        <attr name="awesome_text" format="string" />
    </declare-styleable>

    <!-- 这是模糊背景的属性 -->
    <declare-styleable name="BlurredView">
        <attr name="src" format="reference"/>
        <attr name="move" format="boolean"/>
        <attr name="disableBlurred" format="boolean"/>
    </declare-styleable>

    <!-- 下面这两个是 RecyclerView侧滑菜单，RecyclerView滑动删除，RecyclerView长按拖拽 -->
    <string-array name="main_item">
        <item>左右两侧都有</item>
        <item>根据ViewType显示菜单</item>
        <item>侧滑菜单和ViewPager嵌套</item>
        <item>侧滑菜单和 Refresh LoadMore 嵌套</item>
        <item>长按拖拽Item+菜单(List形式)</item>
        <item>长按拖拽Item+菜单(Grid形式)</item>
        <item>滑动直接删除+拖拽Item</item>
        <item>指定某个Item不能拖拽/滑动删除</item>
        <item>竖型菜单（完美）</item>
        <item>用SwipeMenuLayout实现你自己的侧滑</item>
    </string-array>

    <string-array name="main_item_des">
        <item>可以左右滑动，两遍边都可以滑出来菜单。</item>
        <item>根据ViewType决定菜单在哪一行出现，或者某一行出现不同的菜单，可以左侧，可以右侧。</item>
        <item>RecyclerView嵌套在ViewPager中，解决左右滑动时的冲突。</item>
        <item>侧滑菜单和下拉刷新，上拉加载嵌套</item>
        <item>ListView形式，长按后可以拖拽Item，进行上下移动。和侧滑的Menu菜单结合。</item>
        <item>GridView形式，长按后可以拖拽Item，进行上下左右的移动。和侧滑的Menu菜单结合。</item>
        <item>滑动后直接删除/拖拽Item，不是侧滑菜单那样的删除。</item>
        <item>有的可能想让第一个不能拖拽或者滑动删除，看这个例子。</item>
        <item>打开左右两侧的菜单，成竖向排布。</item>
        <item>用SwipeMenuLayout实现你自己的侧滑，不仅仅用在RecyclerView的Item中，可以用在ListView中，或者其它Layout中。</item>
    </string-array>

    <!-- 圆角图片 -->
    <declare-styleable name="CircleImageView">
        <attr name="border_width" format="dimension" />
        <attr name="border_color" format="color" />
    </declare-styleable>

</resources>